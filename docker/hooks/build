#!/bin/bash

echo "=> Building container: 'scratch' (default)"
docker build \
  --tag $IMAGE_NAME \
  --file scratch/Dockerfile ..

echo "=> Building container: 'debian-slim"
docker build \
  --tag $IMAGE_NAME-debian-slim \
  --file debian-slim/Dockerfile ..
